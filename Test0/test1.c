#include <stdio.h>

void main()
{
	int x; // Declaración de variable
	x = 10; // Inicialización de la variable
	int y = x/2; // Inicialización y declaración
	printf("%i\n",y); //"%i" es un caracter de formato y se usa para declarar a la string como un integer
	;
}

// C is a space insensitive language
